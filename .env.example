# Required: Anthropic API key for AI Coach features
# Get yours at https://console.anthropic.com
ANTHROPIC_API_KEY=

# Supabase — currently hardcoded in src/App.jsx (lines 4456-4457).
# These will move here during the refactor. The anon key is safe to be public,
# but keeping them in env vars makes environment switching easier.
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
